---
title: "NEO"
execute: 
  echo: false
html-table-processing: none
---


```{r}
#| label: setup
source(here::here("_setup.R"))
tool <- "neo"
dat$data <- dat$data |>
  dplyr::filter(grepl(tool, .data$tool_parser))
dat$schemas <- dat$schemas |>
  dplyr::filter(.data$tool == {{ tool }})
```

## Predictions

```{r}
#| label: neo_predictions
tbl_nm <- "neo_predictions"
d <- dat$data |>
  tw_utils$unnest_data(tbl_nm, schemas_all = dat$schemas)
d$res |>
  tw_table$reactable_wrapdown(
    descriptions = d$schema$schema[c("field", "description")],
    id = tbl_nm,
    pagination = TRUE
  )
```

## Candidates

```{r}
#| label: neo_candidates
tbl_nm <- "neo_candidates"
d <- dat$data |>
  tw_utils$unnest_data(tbl_nm, schemas_all = dat$schemas)
d$res |>
  tw_table$reactable_wrapdown(
    descriptions = d$schema$schema[c("field", "description")],
    id = tbl_nm,
    pagination = TRUE
  )
```


::: {.callout-tip}
## TODO

:::
